{
  "name": "nutrition-coaching",
  "version": "1.0.0",
  "description": "Fitness & Nutrition Tracker web app",
  "scripts": {
    "start": "npx serve docs",
    "build": "rm -rf docs && mkdir -p docs && cp -R src/* docs/"
  },
  "dependencies": {
  },
  "devDependencies": {
    "serve": "^14.0.1"
  }
}
